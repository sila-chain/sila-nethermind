[
  {
    "run": "EveryBlock",
    "test": {
      "description": "SRC20 transfers"
    },
    "request": {
      "jsonrpc":"2.0",
      "method":"sil_getLogs",
      "params":[{
        "fromBlock":"{{Hex(Recent.Number)}}",
        "toBlock":"{{Hex(Recent.Number)}}",
        "topics": [
          "{{Topic.Transfer}}"
        ]
      }],
      "id": "{{Request.Number}}"
    }
  }
]
